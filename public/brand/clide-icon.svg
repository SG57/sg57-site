<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <defs>
    <linearGradient id="bg-gradient" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#12151a"/>
      <stop offset="100%" stop-color="#0a0c10"/>
    </linearGradient>
    <filter id="cyan-glow" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur in="SourceGraphic" stdDeviation="8" result="blur"/>
      <feColorMatrix in="blur" type="matrix"
        values="0 0 0 0 0.357
                0 0 0 0 0.753
                0 0 0 0 0.745
                0 0 0 0.3 0" result="glow"/>
      <feMerge>
        <feMergeNode in="glow"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
    <filter id="cyan-aura" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur in="SourceGraphic" stdDeviation="18" result="blur"/>
      <feColorMatrix in="blur" type="matrix"
        values="0 0 0 0 0.357
                0 0 0 0 0.753
                0 0 0 0 0.745
                0 0 0 0.25 0"/>
    </filter>
  </defs>

  <!-- Outer frame: rounded square with gradient background -->
  <rect x="2" y="2" width="508" height="508" rx="80" ry="80"
    fill="url(#bg-gradient)" stroke="#2a3040" stroke-width="2"/>

  <!-- Glow aura behind the prompt symbol (wide, soft bloom) -->
  <g filter="url(#cyan-aura)">
    <path d="M 136 176 L 240 256 L 136 336"
      fill="none" stroke="#5BC0BE" stroke-width="32" stroke-linecap="round" stroke-linejoin="round"/>
    <rect x="272" y="314" width="108" height="22" rx="4" fill="#5BC0BE"/>
  </g>

  <!-- Terminal prompt >_ with crisp glow -->
  <g filter="url(#cyan-glow)">
    <!-- Chevron > — bold geometric path -->
    <path d="M 136 176 L 240 256 L 136 336"
      fill="none" stroke="#5BC0BE" stroke-width="32" stroke-linecap="round" stroke-linejoin="round"/>
    <!-- Cursor _ — simple rectangle -->
    <rect x="272" y="314" width="108" height="22" rx="4" fill="#5BC0BE"/>
  </g>
</svg>
